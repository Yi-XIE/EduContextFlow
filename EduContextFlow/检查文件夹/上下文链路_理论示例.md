# 📘 理论上下文链路示例（完整演示）

本示例以“逐字稿 → 脚本 → 问题链”为一条完整链路，说明上下文如何被索引、读取、注入。

---

## ✅ 场景

用户第一轮：
> “帮我生成一段讲解光合作用的逐字稿”

用户第二轮：
> “根据刚才的逐字稿整理成教学脚本”

用户第三轮：
> “基于逐字稿生成一组问题链”

---

## ✅ 第一轮（生成逐字稿）

### Dispatcher 决策
```
action=call_skill, skill=transcript_generation
```

### Executor 生成文件
```
outputs/transcript.md
```

### Bus 写入 context_index
```json
"transcript": {
  "ref": "outputs/transcript.md",
  "producer": "transcript_generation",
  "status": "ready",
  "description": "教学逐字稿"
}
```

---

## ✅ 第二轮（逐字稿 → 脚本）

### Dispatcher 决策
```
action=call_skill, skill=script_from_transcript
```

### App 组装输入（核心）
App 会从 `context_index["transcript"].ref` 读取文件：

```
=== transcript ===
[逐字稿全文...]

=== 用户要求 ===
根据刚才的逐字稿整理成教学脚本
```

### Executor 执行
```
outputs/script.md
```

### Bus 更新 context_index
```json
"script": {
  "ref": "outputs/script.md",
  "producer": "script_from_transcript",
  "status": "ready",
  "description": "表格化教学视频脚本"
}
```

---

## ✅ 第三轮（逐字稿 → 问题链）

### Dispatcher 决策
```
action=call_skill, skill=question_chain_generation
```

### 关键点：是否注入上下文？
当前 `question_chain_generation` 的定义是：
```
requires_context = []
```
这意味着：
- App **不会**自动去读取 transcript 文件
- Executor 只收到用户当前输入

### 所以：如果你说“根据逐字稿生成问题链”，App 仍然只传：
```
"根据逐字稿生成问题链"
```

**因此会出现“没有上文注入”的感觉。**

---

## ✅ 如何让问题链真正依赖逐字稿？

有两种方式：

### 方式 A（推荐）
把 `question_chain_generation` 的 `requires_context` 改为：
```python
requires_context=["transcript"]
```

这样 App 会自动读取 `outputs/transcript.md` 注入。

---

### 方式 B（临时）
在用户输入中手动粘贴逐字稿文本（不推荐）

---

## ✅ 总结

| Skill | 依赖是否声明 | 上下文是否自动注入 | 说明 |
|------|-------------|-------------------|------|
| script_from_transcript | ✅ requires_context=["transcript"] | ✅ 自动注入 | 正常链路 |
| question_chain_generation | ❌ requires_context=[] | ❌ 不注入 | 会出现“断层感” |

---

**如果你想让“问题链一定基于逐字稿”，必须显式声明依赖。**
